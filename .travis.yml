language: node_js

cache: npm

install:
  - "npm install"
  - "tsc --version"
  - "ts-node --version"
  - "npm run install-packages"

script:
  - "npm run coverage"
  - "npm run coverage-report"
  - "npm run build"
