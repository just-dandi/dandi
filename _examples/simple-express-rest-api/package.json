{
  "name": "@dandi/example-simple-express-rest-api",
  "private": true,
  "scripts": {
    "dev": "node --require ts-node/register/transpile-only --require tsconfig-paths/register ./server.ts",
    "start": "supervisor -e node,js,ts -- --require ts-node/register/transpile-only --require tsconfig-paths/register ./server.ts",
    "build": "sh ./build.sh"
  },
  "devDependencies": {
    "supervisor": "^0.12.0"
  },
  "dependencies": {
    "ejs": "^3.0.1",
    "express": "^4.17.1",
    "pug": "^2.0.4",
    "ts-node": "^8.8.2",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.8.3"
  }
}
