{
  "name": "@dandi/example-simple-express-rest-api",
  "private": true,
  "scripts": {
    "dev": "node --require ts-node/register --require tsconfig-paths/register ./server.ts",
    "start": "supervisor -e node,js,ts -- --require ts-node/register --require tsconfig-paths/register ./server.ts",
    "build": "sh ./build.sh"
  },
  "devDependencies": {
    "supervisor": "^0.12.0",
    "ts-node": "^7.0.0"
  }
}
