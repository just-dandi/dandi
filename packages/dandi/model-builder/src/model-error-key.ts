export enum ModelErrorKey {
  array = 'array',
  map = 'map',
  minLength = 'minLength',
  minValue = 'minValue',
  maxLength = 'maxLength',
  maxValue = 'maxValue',
  oneOf = 'oneOf',
  pattern = 'pattern',
  required = 'required',
  set = 'set',
  type = 'type',
  unknown = 'unknown',
}
